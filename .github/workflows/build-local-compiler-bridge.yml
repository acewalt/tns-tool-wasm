name: Build self-contained TNS compiler (legacy full rebuild)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: Legacy workflow disabled
    runs-on: ubuntu-24.04
    steps:
      - name: Explain current build path
        run: |
          echo "This legacy full rebuild is intentionally manual-only."
          echo "Use the current Finish silent self-contained TNS compiler workflow for release builds."
          echo "It reuses the already validated Ndless runtime and avoids obsolete Windows Boost/linker steps."
